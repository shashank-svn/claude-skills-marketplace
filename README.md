# Claude Skills Marketplace

A marketplace for AI skills, built with React and Vite.

## Setup Steps

To run this project locally, follow these steps:

1. **Clone the repository:**

   ```bash
   git clone https://github.com/Sowjanyashekar28/claude-skills-marketplace.git
   cd claude-skills-marketplace
   ```

2. **Install dependencies:**

   ```bash
   npm install
   ```

3. **Start the development server:**
   ```bash
   npm run dev
   ```

## Environment Variables

Currently, there are no required environment variables for this project.

As development progresses and we integrate with real backends, databases, or third-party APIs (like authentication or payments), any necessary environment variables will be documented here. You would typically create a `.env` file in the root directory and add the keys there based on a `.env.example` file.

_(To be updated as development progresses)_
